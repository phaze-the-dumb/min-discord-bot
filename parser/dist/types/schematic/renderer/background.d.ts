import { Canvas } from 'canvas';
import { RenderingInfo } from '../../util';
export declare function drawBackground({ blockAsset }: RenderingInfo, backgroundCanvas: Canvas, size: number): Promise<void>;
